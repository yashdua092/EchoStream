extends layouts/main-layout.pug

//- any page which extends another template file need to use blocks to render it 
block content 
    script.
        var postId = '!{postId}' // passed in payload 
    .postsContainer

    +createReplyModal(userLoggedIn)

block scripts 
    script(src="/js/postPage.js")